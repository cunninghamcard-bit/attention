{
  "name": "@app/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "The Electron shell: main process + preload + native bridges. Loads the @app/web build output.",
  "type": "module",
  "scripts": {
    "build": "vite build --config main/vite.config.ts",
    "typecheck": "tsc -p main/tsconfig.json"
  },
  "dependencies": {
    "@app/shared": "workspace:*",
    "@app/web": "workspace:*",
    "@electron/remote": "^2.1.3",
    "electron": "^43.0.0",
    "font-list": "^2.1.0",
    "node-pty": "^1.1.0"
  }
}
