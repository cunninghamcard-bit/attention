{
  "name": "@app/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Native-seam port contracts (DataAdapter, ElectronGitApi, ElectronTerminalApi), the typed IPC channel table, and ambient declarations — one definition imported by both the shell and the renderer.",
  "type": "module",
  "exports": {
    "./*": "./*.ts"
  }
}
